# TODO

- [ ] Inspect and fix GIF picker/search term handling in `src/Pages/AllPosts/AllPosts.jsx`
  - [x] Wire `SearchBar` input to `setGifSearchTerm` correctly (no `onClick` misuse)
- [x] Ensure clicking the GIF icon opens the picker and shows trending GIFs by default

  - [x] Clean up GIF retry logic (remove toggle between '' and ' ')
  - [x] Verify GIF selection still inserts URL into post content



- [x] Run lint/build or dev server to confirm no remaining errors

